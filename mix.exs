defmodule ElixirAndPrimes.Mixfile do
  use Mix.Project

  def project do
    [
      app: :getprimes,
      version: "0.0.0",
      elixir: "~> 1.14.0",
      description: "Prime Generators"
    ]
  end
end
